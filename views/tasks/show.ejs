<!DOCTYPE html>
<html>
  <head>
    <title>Task details</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <div class="container">
      <h3><a href="/tasks">Task List</a></h3>
      <h1>Task details</h1>
      <table>
        <tbody>
          <tr>
            <td>Title</td>
            <td>:</td>
            <td><%= task.title %></td>
          </tr>
          <tr>
            <td>Description</td>
            <td>:</td>
            <td><%= task.description %></td>
          </tr>
          <tr>
            <td>Assignee</td>
            <td>:</td>
            <td><%= task.assignee %></td>
          </tr>
          <tr>
            <td>Action plan</td>
              <td>:</td>
            <td><%=task.actionPlan %></td>
          </tr>
          <tr>
            <td>Status</td>
            <td>:</td>
            <td><%=task.status %></td>
          </tr>

          <tr>
            <td>Start Date</td>
            <td>:</td>
            <td><%= moment.utc(task.startDate).add(1,"day").format("MM-DD-YYYY")%></td>
          </tr>
          <tr>
            <td>End Date</td>
            <td>:</td>
            <td><%= moment.utc(task.endDate).add(1,"day").format("MM-DD-YYYY")%></td>
          </tr>

          <tr>
            <td>Restrospective</td>
              <td>:</td>
            <td><%=task.retrospective %></td>
          </tr>
        </tbody>
      </table>
      <h3><a href="/tasks/edit/<%= task._id%>">EDIT</a></h3>
      <form action="/tasks/delete/<%= task._id%>" method="post">
        <button type="submit">DELETE</button>
      </form>
      <button id="backButton" onclick="window.history.back()"> Back </button>
    </div>
  </body>
</html>
